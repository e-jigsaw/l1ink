---
import Layout from "components/Layout.astro";
import New from "components/New.astro";
import { Project } from "components/Project";
const { name } = Astro.params;
---

<Layout>
  <New projectName={name} />
  {name && <Project name={name} client:only />}
</Layout>
